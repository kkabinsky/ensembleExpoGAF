__pycache__/
*.pyc
*.aux
*.log
# but a run log that carries a result the manuscript quotes is a result, not a
# build artefact; Table 4's lower panel is read off window_sweep.log
!outputs/window_sweep.log
!encoding_ablation/output/run_full.log
!encoding_ablation/output/run_budget.log
!encoding_ablation/output/run_lambda.log
*.out
*.tex
# but the LaTeX table a program writes as its result is a result, not a
# build artefact, so it is kept
!backtest_new/output/table_financial_new.tex
*.synctex.gz
verify_all.csv

# Directories the programs write into when run.
# The reference tables the manuscript quotes are tracked under outputs/.
output_supplementary/
output_far_matched/
output_window_check/
Iran_new_run/test_ablation/output/
test_cnn/output/
Iran_new_run/run_pr_auc/*.csv
